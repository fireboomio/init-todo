# 参数注入-@fromHeader
# 从客户端的请求头中取值注入到变量中
# 用 header["token"] 创建一个待做事项（虚构）
mutation MyQuery($title: String! @fromHeader(name: "token")) {
  todo_createOneTodo(data: {title: $title}) {
    id
    title
  }
}