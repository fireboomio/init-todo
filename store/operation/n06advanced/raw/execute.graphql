# 原生更新
# 将用户（id=1）的邮箱更新为"@fireboom.io"
# 参考：https://www.prisma.io/docs/orm/prisma-client/queries/raw-database-access/raw-queries#executeraw
mutation MyQuery($parameters: JSON = ["@fireboom.io",1]) {
  blog_executeRaw(query: "UPDATE user SET email= ? WHERE id= ?", parameters: $parameters)
}