# 通配符过滤
# 单字符通配：_ 多字符通配：% ,用符号 // 进行转义
# 下述示例，会把 邮箱为 @fireboom.io 的用户也查询到
# 参考：https://www.prisma.io/docs/orm/prisma-client/queries/filtering-and-sorting#filtering-faqs
query MyQuery {
  blog_findManyUser(where: {email: {startsWith: "%fire"}}) {
    id
    email
  }
}