# 分页查询
# 获取邮箱包含"@fireboom.io"的第1页的10条用户记录
query MyQuery($skip: Int = 0, $take: Int = 10, $email: String="@fireboom.io") {
  blog_findManyUser(
    skip: $skip
    take: $take
    where: {email: {contains: $email}}
    orderBy: {email: desc}
  ) {
    id
    email
  }
}