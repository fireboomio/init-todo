# 将 所有文章（title包含'fireboom'）的浏览量+1
mutation MyQuery($contains: String = "fireboom") {
  blog_updateManyPost(
    data: {view: {increment: 1}}
    where: {title: {contains: $contains}}
  ) {
    count
  }
}