# 如果用户（email="@fireboom.io"）存在，将邮箱更新为 "default@fireboom.io"
# 否则，创建该用户
mutation MyQuery($email: String = "@fireboom.io") {
  blog_upsertOneUser(
    create: {email: $email}
    update: {email: {set: "default@fireboom.io"}}
    where: {email: $email}
  ) {
    id
    email
  }
}